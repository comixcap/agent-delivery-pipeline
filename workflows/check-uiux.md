---
description: Проверка каждого вью/sheet на UI/UX и цепочки UI→метод→данные→результат
---

## ⚠️ ОБЯЗАТЕЛЬНО ПЕРЕД РАБОТОЙ — прочитай справочники

Это не опция. Правила вынесены из `CLAUDE.md` ради размера, но действуют полностью.
Прочитай эти файлы ДО первой правки кода:

- `$HOME/.claude/rules/layout.md` — вёрстка
- `$HOME/.claude/rules/contrast.md` — контраст и палитра

---
Проведи честную проверку: проверь каждое вью, каждый sheet на лучший UI/UX. Проверь каждую функцию по цепочке: UI-кнопка → вызов метода → сохранение данных → результат виден. Чтобы всё работало — каждая механика и была лучшей по юзабилити для пользователя и для модератора Apple.
